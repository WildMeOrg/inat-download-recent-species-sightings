# MCP Server Dependencies
mcp>=0.9.0

# Image handling: animated GIF frame extraction in the iNaturalist downloader
pillow>=10.0.0

# NLP for date extraction from YouTube video metadata.
# Both are optional at runtime: without them, date extraction falls back to the
# regex-only path and warns, rather than discarding search results.
spacy>=3.7.0
dateparser>=1.2.0

# The spaCy model is a separate download and is NOT installed by this file:
#   python -m spacy download en_core_web_sm
#
# YouTube and Flickr tools use the standard library for API calls (urllib, json).
# Environment variables: YOUTUBE_API_KEY, FLICKR_API_KEY
