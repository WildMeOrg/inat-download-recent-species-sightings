# MCP Server Dependencies
mcp>=0.9.0

# NLP for date extraction from YouTube video metadata
spacy>=3.7.0
dateparser>=1.2.0

# Note: YouTube search tools use Python standard library for API calls (urllib, json)
# Requires YOUTUBE_API_KEY environment variable
# After installing requirements, download spaCy model: python -m spacy download en_core_web_sm

# Optional: For CLIP-based image filtering (if implementing in the future)
# transformers>=4.30.0
# torch>=2.0.0
# pillow>=10.0.0
