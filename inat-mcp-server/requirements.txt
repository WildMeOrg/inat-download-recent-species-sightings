# MCP Server Dependencies
mcp>=0.9.0

# Note: YouTube search tools use only Python standard library (urllib, json)
# No additional dependencies required for YouTube functionality
# Requires YOUTUBE_API_KEY environment variable

# Optional: For CLIP-based image filtering (if implementing in the future)
# transformers>=4.30.0
# torch>=2.0.0
# pillow>=10.0.0
