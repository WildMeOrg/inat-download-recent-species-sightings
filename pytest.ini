[pytest]
# Automated tests live at the repo root and must run with no network and no API
# keys. manual_checks/ holds live-API diagnostic scripts; keep them out of
# collection so a missing YOUTUBE_API_KEY cannot abort the suite.
testpaths = .
norecursedirs = manual_checks .git __pycache__ inat-mcp-server
